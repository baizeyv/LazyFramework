namespace Lazy
{
    /// <summary>
    /// * UI界面状态
    /// </summary>
    public enum PanelState
    {
        Idle,
        ShowAnimation,
        Showing,
        HideAnimation,
        Hide,
        Closed,
    }
}
