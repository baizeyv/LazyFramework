namespace Lazy.UI
{
    /// <summary>
    /// * UI层级
    /// </summary>
    public enum UILayer
    {
        Background, // # 背景层
        PanelLow, // # 低界面层
        PanelHigh, // # 高界面层

        // Dialog, // # 弹窗层
        Guide, // # 引导层
        Toast // # 提示层
        ,
    }
}
